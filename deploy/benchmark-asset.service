[Unit]
Description=Benchmark Asset FastAPI backend
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/benchmarkAsset
Environment=HOST=127.0.0.1
Environment=PORT=8000
ExecStart=/opt/benchmarkAsset/start-production.sh
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
